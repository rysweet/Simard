#!/usr/bin/env bash
# Copilot hook wrapper - generated by amplihack (rust engine)
# Runs multiple hook scripts for this event
INPUT=$(cat)

echo "$INPUT" | /home/azureuser/.local/bin/amplihack-hooks stop "$@" || true

echo "$INPUT" | /home/azureuser/.local/bin/amplihack-hooks session-stop "$@" || true
